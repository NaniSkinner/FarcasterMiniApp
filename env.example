# Supabase PostgreSQL connection string
# Replace with the URI from your Supabase project's database settings
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.xxxx.supabase.co:5432/postgres"

# Redis connection string (e.g., from Upstash)
# We will provision this later
REDIS_URL="redis://..."

# Alchemy API Key for Webhooks, Notify, and SIWE
# You can get this from your Alchemy dashboard
ALCHEMY_API_KEY=""

# Alchemy Webhook Signing Key
# Found on the webhook details page in your Alchemy dashboard
ALCHEMY_SIGNING_KEY=""

# =============================================================================
# EMAIL CONFIGURATION
# =============================================================================

# Option 1: Console Mode (Development - logs emails to console)
# Uncomment this line for development/testing
# EMAIL_MODE=console

# Option 2: Gmail SMTP (Real emails via Gmail)
# Uncomment and configure these for Gmail
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-actual-gmail@gmail.com
SMTP_PASS=your-16-character-app-password
SMTP_FROM=your-actual-gmail@gmail.com

# Option 3: Other SMTP Provider (e.g., SendGrid, Mailgun)
# Uncomment and configure these for other providers
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key
# SMTP_FROM=noreply@yourdomain.com

# Option 4: Alchemy Notify (Production)
# Uncomment and configure for Alchemy Notify
ALCHEMY_NOTIFY_API_KEY=your-alchemy-notify-key

# Default recipient email (where reminders will be sent)
DEFAULT_EMAIL=your-actual-email@gmail.com
